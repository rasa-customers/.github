# Superpowers subagent-driven-development scratch (ledger, briefs, diffs)
.superpowers/

# Local planning notes (personal brainstorming — not for the public repo)
docs/

# Playwright MCP browser session scratch
.playwright-mcp/

# Local research/analytics exports (org traffic data — not for the public repo).
# Track the pull script; ignore the CSV data it produces.
research/*.csv

# macOS
.DS_Store
